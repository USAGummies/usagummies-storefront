# 🌅 Morning Briefing — 2026-04-21

Good morning Ben. Here's what went down overnight + what needs your attention before the pack-and-ship window.

---

## 🚨 Do these first (critical path)

### 1. Top up ShipStation Stamps.com wallet — **$23.84 balance, below $100 floor**
- **Why now:** BUILD #2 preflight will refuse your next USPS label buy until the wallet is above $100 × 1.2 safety.
- **How:** ShipStation UI → Carriers & Fulfillment → Stamps.com → Wallet → Top-up. Recommended: $150-200 to cover the week's Amazon FBM volume.
- **Where to verify:** `/ops/shipping` after reload — stamps_com card should go green.

### 2. Send the Stamps.com refund escalation follow-up — **$130.90 still pending from Viktor triple-buys**
- **Why now:** 10+ days past SLA. Original email sent 2026-04-20. Check Gmail for any reply.
- **If no reply:** reply to the thread with "Following up — please confirm receipt and ETA for the $130.90 credit to account bstutman-1309c."
- **Where:** Gmail inbox (original thread has subject `Refund escalation — 17 USPS labels voided 2026-04-10, no credit on account bstutman-1309c`).

### 3. Configure Amazon FBM webhook alerts — **already cron-scheduled for 09:00 / 13:00 / 16:00 PT**
- `/api/ops/amazon/unshipped-fbm-alert` fires automatically weekdays; first one is today at 09:00 PT in `#operations`.
- **No action needed** unless you want to test it manually: hit the URL with your `CRON_SECRET` bearer from terminal to see the current queue.

---

## 📦 Amazon FBM rolls in today — what's wired

The system now handles Amazon FBM end-to-end:

1. **`/api/ops/amazon/unshipped-fbm-alert`** — polls SP-API every 3 hours during business, posts unshipped MFN orders to `#operations` with urgency tags.
2. **New `mailer` packaging type** — 9×6×2 in, 0.55 lb. Single-bag orders auto-classify as mailer via S-08 classifier.
3. **`POST /api/ops/amazon/dispatch`** — Ben (or a script) copies the ship-to from Seller Central, POSTs orderId + address, gets a Class B proposal in `#ops-approvals`.
4. **`#operations` alert dedupes** — same order only alerts once unless ship-by deadline is <12h away (then urgency re-alert).
5. **Dispatched orders show `:outbox_tray: DISPATCHED — awaiting physical ship`** — stops spam pinging after you've proposed it.

**Flow (browser-driven):**
```
#operations alert at 09:00 PT  →  open /ops/amazon-fbm
  →  each order shows as a card with urgency + Seller Central deeplink
  →  click deeplink, copy ship-to in Seller Central
  →  paste into form, click "Dispatch to #ops-approvals"
  →  Class B proposal appears in #ops-approvals
  →  approve → buy-label fires → label prints
```

**The UI is at `/ops/amazon-fbm`** (under OPERATIONS in the sidebar).
Works on phone + laptop. Auto-refreshes every 2 minutes.

---

## ✅ What shipped overnight (17+ commits)

Total commits this session: 36+ on `main`. Deployment is live. Full detail in `/contracts/activation-status.md` + Notion 22.B.Log.

### New live surfaces
- **`/ops/shipping`** — live wallet / ATP / freight-comp / stale-voids dashboard with recent labels table + inline freight-comp approve buttons
- **`/ops/ledger`** — Rene's one-page finance queue (Approve+Post CF-09 JEs, stale void review)
- **`/ops/agents/status`** — per-agent green/yellow/red health cards (12 live agents)
- **`/api/ops/amazon/unshipped-fbm-alert`** — Amazon FBM queue alerts
- **`/api/ops/amazon/dispatch`** — Amazon FBM → S-08 classifier bridge
- **`/api/ops/fulfillment/packing-slip`** — packing slip HTML renderer (GET for browser print + POST for agents)
- **`/api/ops/fulfillment/summary`** — weekly fulfillment KPI rollup
- **`/api/ops/inventory/cover-days`** — days-of-cover forecast per SKU (Inventory Specialist MVP)
- **`/api/ops/agents/sample-dispatch/dispatch`** — generic S-08 dispatch endpoint
- **`/api/ops/webhooks/shopify/orders-paid`** — Shopify webhook → S-08 classifier (HMAC-verified)
- **`/api/ops/webhooks/hubspot/deal-stage-changed`** — HubSpot webhook → S-08 classifier (signature-v3 verified)
- **`/api/ops/control-plane/fulfillment-drift-audit`** — weekly Mon 20:30 PT drift scorecard → `#ops-audit`

### New cron entries (14 total in `vercel.json`)
- Mon 03:30 UTC — fulfillment drift audit → `#ops-audit`
- Weekdays 09:00 PT — ShipStation wallet + void watcher → `#operations`
- Weekdays 09:00 / 13:00 / 16:00 PT — Amazon FBM unshipped alert → `#operations`

### New tests
- Classifier (S-08): **18/18 green**
- Shopify normalizer (S-08): **12/12 green**
- HubSpot normalizer (S-08): **15/15 green**
- Packing slip renderer: **11/11 green**
- **Total new: 56/56 vitest green this session.**

---

## 🛠️ What you need to do manually (env + UI config)

All of these unblock agents or close loops that are already code-built:

| # | Item | Where | Blocker unblocks |
|---|------|-------|------------------|
| 1 | **Top up Stamps.com wallet** | ShipStation UI → Stamps.com → Top-up $150+ | Your next USPS label buy |
| 2 | **Configure Shopify `orders/paid` webhook** | Shopify Admin → Settings → Notifications → Webhooks. URL: `https://www.usagummies.com/api/ops/webhooks/shopify/orders-paid`. `SHOPIFY_WEBHOOK_SECRET` env already set. | S-08 dispatches Shopify orders automatically (no more manual buy-label) |
| 3 | **Configure HubSpot `deal.propertyChange` webhook** | HubSpot Settings → Integrations → Private Apps → Webhooks. Subscribe to `dealstage`. URL: `https://www.usagummies.com/api/ops/webhooks/hubspot/deal-stage-changed`. Set `HUBSPOT_APP_SECRET` env on Vercel. | S-08 dispatches HubSpot deals on stage → PO Received / Closed Won |
| 4 | **Set `HUBSPOT_APP_SECRET` on Vercel** | Vercel → usagummies-storefront → Settings → Environment Variables | Signature verification on HubSpot webhook |
| 5 | **Set `FAIRE_ACCESS_TOKEN` on Vercel** (optional — when you have Faire volume) | Vercel env | Faire Specialist → live mode |
| 6 | **Set `BOOKE_API_TOKEN` on Vercel** OR wire Zapier bridge to `/api/ops/booke/push` | Vercel env OR Zapier scenario | Finance Exception Agent "Uncategorized transactions" line |
| 7 | **Create Notion `/Legal/Compliance Calendar` database** — minimum cols: Name (title), Due (date), Owner (text), Status (status), Category (select) | Notion | Compliance Specialist → live mode (currently uses [FALLBACK] doctrine list) |
| 8 | **Tune `INVENTORY_BURN_RATE_BAGS_PER_DAY` env** (currently default 250) | Vercel env | Cover-day forecast accuracy |
| 9 | **Tune per-SKU burn rates** via `INVENTORY_BURN_RATE_<SKU>` envs | Vercel env | Per-SKU stockout projections |
| 10 | **Verify ShipStation webhook ID 106614 is still active** | ShipStation UI → Settings → Integrations → API | Tracking auto-clear flag |

---

## 🔍 Known issues / things to watch

- **Vercel cron count = 14.** Hobby plan historically capped at a lower number; monitor the Vercel deployment log for cron rejections. Fallback: migrate 2-3 to Make.com.
- **Inventory burn rate is a placeholder** (250 bags/day). The cover-day forecast will be misleading until you set a real number based on your actual sales velocity.
- **Amazon dispatch route requires PII manual entry** — SP-API requires Restricted Data Token (RDT) for buyer addresses. MVP asks you to copy from Seller Central. Can layer RDT later if Amazon volume justifies the PII-approval burden.
- **Compliance fallback lists categories, not dates** — you still need to populate the real Notion Compliance Calendar with actual due dates from your Wyoming Attorneys LLC filings.

---

## 📊 Session metrics (2026-04-20 → 2026-04-21)

- **Commits on main:** 36+
- **New API routes:** 15
- **New UI pages:** 3 (`/ops/shipping`, `/ops/ledger`, `/ops/agents/status`)
- **New lib modules:** 12
- **New vitest tests:** 56 (all green)
- **New contracts docs:** 2
- **Live agents:** 12 (up from 9 going in)
- **Packaging types:** 3 (mailer, case, master_carton) — was 2
- **Stamps.com escalation email:** 1 sent, 17 tracking numbers listed, $130.90 in flight

---

## 🎯 Recommended first 30 min

1. **[2 min]** Top up Stamps.com wallet
2. **[5 min]** Check Slack `#operations` + `#ops-daily` for overnight alerts
3. **[5 min]** Check Gmail for Stamps.com reply; follow up if needed
4. **[5 min]** Configure Shopify webhook (one-time, 5 min)
5. **[5 min]** Configure HubSpot webhook (one-time, 5 min)
6. **[remainder]** Start Amazon FBM shipping — open `#operations`, watch for the 09:00 PT alert, ship the first few orders through the flow

Once #2-#5 are done, the system auto-handles most of the daily flow. Amazon FBM shipping is the only thing requiring your manual ship-to copy/paste (until RDT).

Good luck today 🇺🇸
