// Reviews — removed. The three quote cards in the previous version were
// fabricated. This block is kept as a structural placeholder so Ben can
// drop in verified Shopify / Amazon review quotes when they're ready.
// Until then, we render nothing so the page doesn't publish fiction.
//
// To re-enable: export the component from the page, paste real verified
// review text into VERIFIED_QUOTES below, and uncomment the grid.

export function ReviewsStrip() {
  return null;
}

// Keep the template ready for when real reviews are in hand.
// type Quote = { stars: number; quote: string; name: string; where: string; date: string };
// const VERIFIED_QUOTES: Quote[] = [];
